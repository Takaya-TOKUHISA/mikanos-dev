# MikanOS 開発記録

『ゼロからのOS自作入門』を進めながら、OSの仕組みを学習するリポジトリ
## 環境
- **OS**: Ubuntu20.04(WSL2)
- **Editor**: VS Code (Remote WSL)
- **Emulator**: QEMU
## 進捗

<details><summary><big><b>1日目(2026-03-01)，第一章</summary></big></b>

- **やったこと**
    - バイナリファイル'BOOTX64.EFI'による"Hello, world!"の表示命令の作成．
    - 'BOOT64.EFI'の配置，QEMUでの起動．
    - C環境での"Hello, world!"の表示命令の実現とQEMUでの表示．
- **学んだこと**
    - 文字の表示には空ファイルをFAT形式でフォーマットし，そこにバイナリファイルを書き込む．
    - EDK II環境をとりいえるためにansibleを用いたが，githubのバージョン管理機能をおろそかにしており，Ubuntu20.04に対応するバージョンではなく，エラーに見舞われた．
- **考察**
    - 今までのCのコンパイルはgccで行っており，これは対象のOSに沿った形にコンパイルされる．今回は作成したOS(Ubuntu20.04)である一方実行ファイルのルールはwindows形式であるため，windows向けのバイナリファイルを作成する必要があるという部分で少し複雑に見えると考えた．
    - gccではmainから始めるが，今回はEfiMainを指定してスタートする位置を自分で決めている．
---
</details>
<details><summary><big><b>2日目(2026-03-02)，第二章</b></big></summary>

- **やったこと**
    - EDK IIを用いたハローワールドアプリの作成．
    - そのためのエントリポイント等の設定と，EDK IIでビルドするための設定．
    - buildを用いた機械語への翻訳．
    - メモリマップを取得するプログラムの作成．
- **学んだこと**
    - １日目ではEDKを用いず，直接ライブラリを書き込むことで実現したが，ヘッダファイルを記述してUEFI等の機能をインクルードすることでコードを省略できた．
    - buildコマンドを用いる内部では，.dscファイルを読み込んでプログラムの全体像を把握し，
    .infファイルを読み込むことでソースコードやプロトコルなどを把握する．
    そしてそのリストに従ってコンパイラ，リンカを動かして.oや.dllに変換し，最終的にUEFIが読める.efiファイルに変換する．
    - gEfiの様々なプロトコルを活用することで，memmapというメモリの全体図を記録するCSV形式のファイルを作成し，各領域の状態を確認する方法を得た．
    - git diffやdiffのを使って自分のコードとお手本の差分を確認し修正する手法を得た．
    - コードを更新したにも関わらず，それに対応するプロトコルのguidを.infファイルに書き忘れてエラーに見舞われた．
- **考察**
    - グローバルな変数等は先頭にgをつける関数がある．
    - 今回得たメモリマップ情報は今後メモリ書き込みなどの更新の際に書き込み可能な領域を確認するために活用されると考えられる．
---
</details>
<details><summary><big><b>3日目(2026-03-03)，第三章</b></big></summary>

- **やったこと**
    - c++でカーネルを作成し，ブートローダからカーネルを呼び出した．最初の段階としては，待機時間中にbusy loopにならないようにアセンブリのhlt命令を呼び出し，待機状態を実現した．
    - カーネルはディスクから呼び出すのではなく，メモリ上から呼び出した．
    - フレームバッファを用いてピクセルを描画した．GOPを取得して全領域の値を255とすることで背景を白色にした．
    - カーネルにフレームバッファの先頭アドレスとサイズを渡し，待機命令の前にカーネル上でピクセルを適当に描画した．
    - これまで作成した全関数にEFI_STATUS statusを追記し，そして各命令ごとに返り値をstatusに格納し，エラー処理を行った．
- **学んだこと**
    - qemuのCLIが，ブートローダの処理がすべて終わる前から操作可能であるため，info registersでレジスタの状態を確認したときに想定の状態と異なっており，処理が終わっていないことに気が付くまでに時間がかかった．
    - これまでエラー処理をしてないことが気がかりだったのでやっとエラー処理が考慮されたコードをかけた．
    - CやC++では，short, int 等の型の大きさが定まっていないため，uint64_t等の定まった大きさの変数を使うことが重要である．
    - ピクセルのフォーマットにはRGB以外にもBGRやマスクなどの様々な手法があることが分かった．そしてBRGが結構使われているらしい．
- **考察**
    - 各ピクセルに逐一値を格納して描画するのはディスプレイによっては実は結構重い処理なんじゃないかと感じた．
    - EFI_FILE_INFOのメンバFileName[]は領域が定まっておらず，利用する際にこちらで適切に設定する必要がある．今回は愚直にファイル名の文字数を数えてハードコードしていたが，一文字増えたりしたら読み込めなくなる危険な手法なのではと考えた．
    - statusを作成してエラー処理を行ったが．if(EFI_ERROR(___))の中に処理を直接書き込むという手法もなしではないと思う．ただ，引数が多かったりすると異様に見づらくなる．
    - メモリ確保にはAny，Max，何もなしの3つの関数が用意されている．それぞれどこか空いている場所，指定したアドレス以下で空いている場所，指定したアドレスを確保の処理である．Anyは一般的なデータ等，カーネル本体等よりは重要ではなく，どこでもとりあえずおけたらいいよ程度のデータのための領域確保に使える．
    Maxのほうはあまり使い道が分からないが，32bit機器との通信だと大きすぎたら扱えない場合があるらしいので，それに使うらしい．
---
</details>
<details><summary><big><b>4日目(2026-03-04)，第四章</b></big></summary>

- **やったこと**
    - Makefileを作成することで，事前にコンパイルやリンクの条件を設定しておいて，makeコマンドを打つことで，カーネルの.elfファイルを作成できるようにした．
    - ピクセルのフォーマットを考慮した描画を行うために，ブートローダで必要な情報をカーネルに送信し，カーネルでフォーマットに合った出力形式で描画するようにした．
    - ピクセル描画のための共通部分を保持したクラスと，それを継承したRGBとBGR用のクラスをカーネルに作成し，各クラスで同名の描画関数を定義することで，関数内でのフォーマット判定を回避した．
    - 今のローダは必要な領域の計算が誤っていたため，正しい計算式を実装した．
- **学んだこと**
    - makeにはdockerファイルと似たものを感じた．
    - 最初は各関数が呼び出されるたびにピクセルのフォーマット判定という無駄な処理を挟んでいたが，
    クラスを実装して関数呼び出し以前で仕分けることで，無駄な処理を減らせており，このような処理の積み重ねが軽量化につながるのだと感じた．
    - 今までは単純にファイルサイズをもとにメモリを確保していたが，これではLOADセグメントの.bssセクションが無視されてしまうため，より詳細に先頭アドレスと終了アドレスを計算して適切なサイズを獲得することが必要だとわかった．これをしないと.bssのための物理メモリが足りなくなってしまう．
- **考察**
    - 参考書では面倒だという理由でBitMaskとうのフォーマットの実装をしていなかったので，自分でアルゴリズムを理解して処理を付け加えてみたいと考える．
    - メモリ管理の時にいっそのこと余分にとれば.bssセクション分は取れるのではないのかと考えた．
    普通のアプリ開発であれば未初期化変数の数はそれで十分な量であることが多いが，今後実装するであろうページ表など巨大な配列を未初期化変数として宣言することがあるので，それではうまくいかないらしい．
    - ページ表は実際今の64bitプロセッサでは４段，５段と段数がどんどん増えていっているようなので，綿密に計算することがメモリ管理には必要といえそう．
---
</details>
<details><summary><big><b>5日目(2026-03-05)，第五章</b></big></summary>

- **やったこと**
    - フォントをバイナリデータとして自作して，それをピクセル描画を用いて描画した．
    - フォントと画面描画，メイン処理と複雑になってきたので，それぞれを別のファイルに分割して保存し，それらをインクルードすることで可読性を上げた．
    - [東雲フォント](http://openlab.ring.gr.jp/efont/shinonome/)を加工して，フォントデータを組み込み，画面に描画した．
    - 一文字ずつは描画できるようになったので，sprintfを用いて文字列を作成し，それを描画するための文字列描画関数を作成した．
    - コンソールクラスを作成し，25行分の文字データを表示できるようにした．
    - これらの機能をまとめ，printfのように扱える文字列を出力するための関数printkを作成した．
- **学んだこと**
    - 文字だからと特別なことはなく，ただ，与えられた情報に沿ってピクセルを塗りつぶしているだけであるとわかった．
    - フォント一つを作るのにも結構苦労するので，漢字等の実装は外部のフォントを変換する以外ではしんどそう．
    - 一つの文字を出力する関数を，与えられた文字数分ループすることで文字列描画は実装できたが，自分がCの演習等でやってきたことと似て愚直な処理であると感じた．
    - ...のようにして引数に自由度を持たせられることを初めて知った．これを用いてフォーマットの数に依存しない文字列出力関数の実装ができた．
- **考察**
    - ピクセル描画で文字表示していることが分かったが，フォントサイズが各文字で異なると文字列ががたついたり，ほかの文字と重なったりする可能性があるのではないかと考えた．また，これに対応するためにフォントの大きさの正規化関数を実装してみても面白いかもしれない．
    - 文字列出力関数を実装したが，フォーマットと引数の数の不一致によって，未定義動作となってしまう可能性があるので気を付ける必要がある．
    - 文字列出力関数を実装できたので，デバッグなど様々なことが可能になったと感じている．
---
</details>
<details><summary><big><b>6日目(2026-03-06)，第六章</b></big></summary>

- **やったこと**
    - マウスカーソルの描画をするためにカーソルの形を定義した．
    - 背景を作成するために長方形の色塗りと枠線を塗るための関数群を作成した．
    - USBドライバを利用して，接続されているPCIデバイスの探索を行い，列挙できるようにした．
    - 出力する文字列にレベルをつけて，実際に出力する必要のある文字列だけを画面に出すように絞り込めるLog関数を作成した．
    - そのうち，xHCI(USB3.0)のUSBマウスを探索して，実際にその動きに追従する形で画面上のポインタを動かした．
- **学んだこと**
    - カーソルもピクセル単位で設定して描画していた．
    - PCIバスやその中のファンクション，デバイスのヘッダ等のデータを活用して特定の機器を探索することで，少しだけでもUSBなどのPCIデバイスの構造と内部での各機器の種類の判定原理を理解することができた．
    - メモリアドレス空間上にメインメモリ以外のメモリのように扱える部品も接続されていることを知った．xHCを制御するMMIOのアドレスをBAR0という領域から取り出して利用した．
    - 同じマウスであっても，UBS2.0やUBS3.0など，通信のプロトコルが異なる場合が往々にしてあるので，これらについて常に考え続けなければならないOS側の苦労を知った．
    - 今回実装した段階では実際にマウスを動かしてからマウスカーソルが動くまでの時間に遅延を感じたり，マウスの動きにカクつきを感じた．
- **考察**
    - 単なるカーソルの描画は実現できたが，以降で文字入力状態やタブ操作時など様々なカーソルの状態を表現する際の，状態切り替えの実現がなされるのかが気になる．
    - 今回の実装ではI/Oに対して待機を行わないビシーループな形のポーリングでの実装となったが，次章</b></big></summary>では割り込みを用いたマウスの処理を行うらしいが，割り込み処理を実現する際には必ず考えなければならない，同一資源に対しての排他制御をどのように実装するのか気になる．
    - プロトコルの違いはこれまでに出てきた画面描画フォーマット形式にも言えることであり，無法にプロトコルが乱立する状態でこの世にある手法全てに対応しようとすれば果てがない．
    そこで，標準規格の存在によってOS側で対応すべきプロトコルが制限され，ほとんどの機器が互いに適切に通信できているのだと考えた．
    - 実用例は挙げられなかったが今回実装したLog関数の考えかたは，非常に有用なもので，必要な情報だけに絞って表示するのが便利に感じた．
    また，実際にデバッグの際にレベルを変更して問題点を探すことができたので，これ以外の自分の開発環境でも利用することで，デバッグが容易になるのではないかと考えた．
---
</details>
<details><summary><big><b>7日目(2026-03-07)，第七章</b></big></summary>

- **やったこと**
    - 割り込みハンドラを実装して，マウスの監視をポーリングから割り込みに変更した．
    - 割り込みハンドラを高速化するために，割り込みの記録のみをハンドラで処理し，それ以降の本格的な処理は後でゆっくりと行う．
    - これを実現するためにキュー(FIFO)を実装し，時系列に沿った順に矛盾なく記録処理できる構造とした．
- **学んだこと**
    - 割り込みにも方式があり，今回は xHCI が対象であるので MSI 割り込みという比較的新しい手法を利用していることが分かった．
    - メインメモリでは，書き込みが行われただけではそれがCPUに伝わらないが，0xfe00000から，0xfe00400までの範囲はレジスタ上に配置されており，書き込みをトリガーとする処理が行えるとしった．
    - これまではメッセージがなくても常にループを回し続けていたが，今回で割り込みハンドラ，キューを実装したことで効率化を図っていることが分かった．
    - 今回は"cli"による簡単なデータ競合の阻止を行った．これによって，同時な同一資源へのアクセスを阻止し，データの整合性を維持できているとわかった．
    - "cli"だけでは完全にデータ競合が阻止できないことも分かった．
- **考察**
    - 今回の手法ではキューのメッセージを確認して，なければ"hlt"命令によって休止状態に変化させた．これによってOSが省電力になり，この間は他のタスクに資源を譲渡できるなど効率的な資源管理ができている．
    - 割り込みハンドラは予測できない入力と相性がいい一方で，その間は資源を独占するという欠点がある．そのため，今回実装したような割り込みが必要な処理とそうでない処理の判別による割り込み時間の最小がが重要であり，性能に直結すると考える．
    - 割り込み記述子テーブル(IDT)に割り込みハンドラを登録することで，マウス等の割り込み要因番号とそれに紐づく割り込みハンドラのアドレスの連想テーブルとなっており，ハードとソフトの窓口として機能しているといえるのではないかと考える．
---
</details>
<details><summary><big><b>8日目(2026-03-08)，第八章</b></big></summary>

- **やったこと**
    - 二日目にやったUEFI BOISで取得したメモリマップをKernelMain()に渡せるようにした．
    - UEFIが作成したスタック領域，GDT，ページテーブルをOSが管理できる領域に移動させ，適切にメモリ領域の状態を判別できるようにした．
    - 簡単なメモリマネージャを作成し，4KiBページフレーム単位でメモリ領域を管理できるようにした．
- **学んだこと**
    - 複数言語間で同じファイルを参照する際に#ifdefをつけることで片方のみそれを利用するように制限できると知った．
    - UEFI起動時に作成するデータ構造はOS側では未使用と判定されてしまうため，ブートローダが起動して適切なタイミングで，それらの領域をOSの管理下となる領域に移動させなければならないことが分かった．
    - これまででもアセンブリを使ったコードはあったが，メモリ等の資源管理を直接OSで行おうとする際には，
    高級言語であるC++では行えず，アセンブリを用いてretfやlgdt等の命令を行わなければならないとわかった．
    - また，これらのためにはエントリポイントをアセンブラ側で定義しておき，そちらで新しいスタック領域を定義してから本来のエントリポイントに移動するという動作が必要であるとわかった．
    - 今回は実際に4段のページング構造を設定したが，やっていることは2段のページングと変わらず，多段にすることは，例えるならば完全木の高さを高くしているだけのことだと感じる．実際のページングの実装はまだらしい．
- **考察**
    - 今回はAllocateでループを用いて必要なフレーム数分連続した空き領域を探して割り当てるFirst Fitを利用した．これでは，使用領域が増えると空き領域が見つからず，時間計算量がO(N)に近づく，そのため，初めから，空き領域のみを管理するFree Fitなどの管理方法が考えられる．
    - 空き領域が連続である必要があることで，小さな数のフレームが大量にまばらに空いている際に，総空き領域は足りていても領域を確保できないことが考えられる．そのため，この解決策として，離れた領域でも仮想的に連結させて処理する方法が考えられる．また，処理の重さに目をつむると，定期的に各状態で固まるよう領域の再配置を行い，空き領域とそうでない領域の境界を定義する手法が考えられる．
    - 今回はフレーム単位で管理しているため，4KiB単位でしか領域を確保できず小さいデータが大量にメモリ確保を要求するとその領域の大部分が無駄になる．そのため，各フレームで複数のオブジェクトを管理できる仕組みが必要である．
    - 今回のAllocateだけでは確保した領域が足りなかった場合に手動で解放してからもう一度適切な領域を確保する必要があり，動的な資源を管理する際に不便である．そのため，既にある領域に追加して領域を確保するなどのすでに取得した領域への操作があると便利だと考える．
---
</details>
<details><summary><big><b>9, 10日目(2026-03-09~10)，第九章</b></big></summary>

- **やったこと**
    - レイヤ構造を作成し，背景レイヤとカーソルレイヤを重ね合わせることで，カーソルの移動による背景の破壊を対策した．
    - 処理の高速化を判別するためのタイマー関数群の作成を行った．
    - フレームバッファへの memset を用いた画面描画の高速化を行った．
    - コンソールのスクロール処理を，各行の再書き込みから memset を用いた2行目から最終行の一括コピーペーストと最終行への書き込みに変更することでWrite()の呼び出しを少なくして高速化した．
- **学んだこと**
    - いつのまにかにビルドをするためのtarget.txtを書き換えていて，QEMU 再起動しまくる問題に見舞われた．
    - そのレイヤでは使われない色を透過色として指定することで，その色に到達したときに下のレイヤを探索して最初に出てくる透過色でないレイヤの色を採用することで，背景の破壊を防いでいるとわかった．
    - このレイヤ構造は紙に上から色(レイヤ)を重ねて塗っているというイメージ
    - 使いごこちを考えると，細かな部分でもできる限り高速化を行うことが大切だとわかった．
    - 前考えていたように各ピクセルへの処理が重くて，それをピクセルフォーマット形式を利用した memset で一括で処理を行うことで高速化できるとわかった．
    - また，描画処理の高速化には同様の形式のバッファを用意してこちらで全部処理してから一括で渡すことで呼び出し回数を削減し，呼び出しを繰り返すことで起きる遅延を削減できるとわかった．
- **考察**
    - 高速化のために自前でバッファをメモリ領域に用意しておくということは，その分メモリ領域を圧迫してしまうということであるので，必要なバッファの大きさと現在のPCのメモリ容量との兼ね合いを考えて実装する必要がある．
    - 今回行った高速化の手法は，大学の講義で扱っていた標準入出力の原理に近しいものを感じた．標準入出力では，システムコールが遅いため，入出力をバッファにためてから一括でシステムコールを用いるという考え方で，こちらは型変換を行ったうえでメモリへの書き込みを膨大な量繰り返すことが遅延の原因だと推定して，処理の終わった自前のバッファの存在するメモリ領域をフレームバッファに memcpy で転送している．
---
</details>
<details><summary><big><b>10日目(2026-03-10)，第十章</b></big></summary>

- **やったこと**
    - これまではカーソルの移動制限がなく左右の端から逆側へワープできてしまっていた．カーソルの移動域に制限を付けた．
    - 重ね合わせを用いてタスクウィンドウを作成した．
    - タスクウィンドウ上に動きのあるな文字列を置くため，メイン内でのループ回数をカウントして描画した．
    - カウントの描画を高速で繰り返すことによる画面のちらつきを抑えるために，再描画を行う範囲を制限して必要な処理のみで描画するようにした．
    - 今度はマウスカーソルをタスクウィンドウ上に置くとカウンタの描画中にマウスが描画されずちらつくため，バックバッファを作成してバックバッファへの書き込みが完了してからフレームバッファに転送することでマウスカーソルのちらつきを解消した．
    - ウィンドウを左クリックしながらカーソルを移動させることでタスクウィンドウをドラッグできるようにした．
    - そのままではコンソールも移動可能なウィンドウとなってしまうので，ドラッグ可能かを表すフラグを作成した．
- **学んだこと**
    - 今回はバッファを使うことで，常に画面を書き換え続けることで起きるちらつきを解消した．重ね合わせ構造を実現していることで，ピクセル描画をレイヤごとに下から行う必要があり上のほうのレイヤはその時間再描画を待つ必要があり，その時間は下のレイヤが表示されてしまう．今回はバッファを重ね合わせ完了までデータを保持しておく場所として活用し，１画面が描画し終わってからフレームバッファに転送することでちらつきを抑えている．一方で本来必要ないバッファへの書き込みがあるため，その分実行速度が低下してしまった．
    - 今回のクリックした場所の最前面エリアの探索の実装で，ラムダ式による探索の有用性が感じられた．ラムダ式を用いない場合，引数が一つに制限されるが，ラムダ式のキャプチャを用いることで，3変数を容易に引数として利用でき，わかりやすい実装となる．
    - 正しい関数群を実装しているのに make 時に /usb 内のコンパイルがうまくいかず，少し手間取った．
- **考察**
    - 今回のタスクウィンドウの実装で実際のパソコンに非常に近づいた感覚があるが，まだ×ボタンが機能していないので，タスクウィンドウの削除機能はない．しかし，この実現方法はクリックした座標の検知とその座標上の最上面レイヤ探索と，重ね合わせ機能によるウィンドウの階層の移動や削除を用いることで実現できる気がする．
    - 今回は左クリックが一般に1で判別されていることから，0x01と入力の積をとって判別したが，0x02や0x04等で右，中央ボタンの判別ができるので，マウスだけできることが格段に広がったように感じる．
    - コンパイルがうまくいかなかった理由として，/usb/classdriver/mouse.cpp, mouse.hppの編集はMakefileで確認され，コンパイルされたものの，make cleanでそれに依存するコードの再コンパイルが削除されておらず，その間で実装の齟齬が検出されたからであった．そこで，make cleanして rm -rf \*.o を実行したが，これでは/usbディレクトリ下に存在するdevice.oは削除対象にならず，コンパイル時にエラーが出ることが分かった．そこで，\*.o を $(OBJS)にして，必要なすべての.oファイルが記述されている成果物を削除対象とし，網羅的に削除するファイルを指定した．これで解決したが，根本の原因としては，そのコードが実行するヘッダファイルの更新は条件としてMakefileで指定されておらず，再コンパイルされないことである．
    この解決策として，各コードが依存するヘッダファイルを条件として列挙することがあるが，
    それぞれのコードが 1:多 で一方的に依存しているため，それを全部列挙するのは非現実的な気がする．
    安パイとして今後は毎回 make 前に make clean を挟もうと思う．
---
</details>
<details><summary><big><b>11日目(2026-03-11), 第十一章</b></big></summary>

- **やったこと**
    - カーネルメインのコードに様々な役割の処理が点在してごちゃごちゃしているので，それぞれを関数にまとめて，その役割に近いヘッダファイル，コードに委譲した．
    - 実行時間の計測を，ポーリングを使って行っていたため，それを短期間の周期的な割込み処理とその回数のカウントによって実現した．
    - 複数の論理タイマーを優先度付きキューに記録し，それらのタイマーののタイムアウト時間を参照し，タイムアウトしていればその時刻
    を含む通知を送るようにした．
    - 現在実装している，Local APIC タイマは各PCのtick間隔に依存しており，それらは大きくばらつきがあり，1カウントの秒数が分からない．そのため，ACPI PMタイマという動作周波数が分かっているタイマを基準として，カウントの周期を測定する機能を作成するため，
    そのRSDPを取得した．
- **学んだこと**
    - やっぱり一つの関数には特定の役割と，それ以外は関数呼び出しを用いた実装にするべきであって，そうすることで，デバッグもしやすくなるとわかった．
    - 今までのタイマはポーリングで，タイマ実装時に __asm__("sti/n/thlt")から，__asm__("sti")に代わっていて，これが機能的に退化してないか不安だったが，やっぱり当たっていた．
    - ちゃんとhltを入れて，割り込みを用いたタイマにすることで，タイマの計測頻度は落ちたものの，CPUへの負荷は低下し，実用可能なレベルでの低下であるといえる．
- **考察**
    - 現在時のタイマでは，優先度付きキューによる，非常に効率的な複数タイマ管理が実現されている．タイマ処理は排他制御のいる，割り込みハンドラ内での処理であり，この部分の実行速度がそのほかの機能にも多大な影響を与える．優先度付きキューでは，残り時間が少ないタイマから順にソートされており，ソートされていない場合にはタイムアウト確認で全タイマを確認しなければいない一方で，最初にタイムアウトしていないものに出会うまで pop() し続けて，出会えば break; して終了となり，ループ回数を大幅に短縮できると考えた．
    - 優先度付きキューで効率的な管理が実装できた．実際にはほとんどないが，すべてのタイマが同時にタイムアウトしているときの実行時間はソートしていない場合と同じである．この時，その役割を持つ Tick() 関数は全タイマをループするため，実行時間はO(N)となる，
    この間は pop 等の操作を行うため割り込みが禁止されることを念頭に置く必要がある．
--- 
</details>
<details>
<summary><big><b>12日目(2026-03-12), 第十二章</b></big></summary>

- **やったこと**
    - タイマーをACPI PMに変更し，100カウントが現実の一秒と対応するように調整した．
    - キーボードドライバを利用して，キーボードから受け取ったキーコードを読み取ってそれに対応する文字を画面に表示できるようにした．また，シフトを利用したもでファイヤキーの作成による入力文字の切り替えも実現した．
    - カーソルとバックスペースが入力された場合の処理を追加し，現在の入力位置とその左の文字の削除を行えるようにした．
- **学んだこと**
    - タイマーをACPI PMに変更し，10msec単位ではかれるようにしたが，これをより詳細化することも可能だが，その場合にはタイムアウトの回数が増えてCPUにその分の負荷がかかることも考えなければならないとわかった．
    - 結局テキストボックスの作成と文字の入力も，今までにやったタスクウィンドウの表示とフォントとキーコードの対応付けをすればいいだけで，これまでに作った機能が基礎としてしっかり動いていることが確認できた．
    - カーソルの点滅機能についても，タイマを用いてタイムアウトをフラグとして，カーソルの点灯と暗滅を切り替えて描画しなおせばいいだけで，ちょうど作ったタイマ機能も役に立っていた．
- **考察**
    - テキストの入力は，既に作成されたキーボードドライバを利用した実装だったため，味気なかったが，そのキーボードの認識までの処理はPCIバスからの検出とヘッダの分析で実現できていると思う．
    - カーソルの移動機能は実現されなかったが，これの実現には３つの機能を用いることで実現できると思う．それは，
        - 重ね合わせを用いたカーソルレイヤのテキストボックスレイヤの一つ上の階層への配置．
        - 文字の挿入削除の際に，上下スクロール機能を応用したカーソルを起点とした左右への文字列スクロール．
        - キーコードを読み取ることによる移動キーのキーコードの確認とその検知．

        これらを組み合わせることで，既に文字がある位置にカーソルが移動しても暗滅時には文字を透過し，そして文字の削除・追加時にはスクロールを行うことで書き込みの遅延を削減ができる．また，カーソル移動の根本となるキーコードの確認と，その検知は今回実装機能に簡単に追加できると考える．
---
</details>
<details>
<summary><big><b>13日目(2026-03-13), 第十三章</b></big></summary>

- **やったこと**
    - アセンブリを用いて各タスクの処理を一定間隔ごとに切り替えるコンテキストスイッチを作成した．
    - 協調的マルチタスクからプリエンプティブマルチタスクに切り替えた．
- **学んだこと**
    - 今回のコンテキストスイッチの実装によって，タスクごとに独立してコンテキストを保持できるようになった．
    - 協調的マルチタスクとプリエンプティブマルチタスクがあると知った．協調的マルチタスクではバグのあるアプリが一つでもあってそれがCPUの譲渡を行わない場合そこで処理をし続けてしまい破綻する．プリエンプティブでは，タイマによってCPUの割り込みによってタスクの切り替えを行うため，タスク側で切り替えを気にする必要がない．
    - タスクの切り替えといっても，そのタスクが持っているコンテキストをアセンブリで，丸ごと別の場所に退避させて，その代わりに動かしたいタスクのコンテキストを丸ごとその部分に移植するだけで実現できるとわかった．
- **考察**
    - マルチタスクとはいっても，処理をするCPUは一つなので，タスクの切り替え時間をちょうどいいように調節する必要がある．一区間ごとの処理時間を延ばすとまとめて処理できる一方で表示が変わらない時間が長くなる，区間を短くするとリアルタイムに動かせるが，コンテキストの退避と復帰のプロセスの試行回数が増えて，結果的に本質的な処理に避ける時間が短くなると考える．
    - 今回の実装後，タスクBを高速でドラッグすると，タスクウィンドウとマウスの軌跡が残ってしまうようになった．この原因として，メインタスクとタスクBの間で排他制御が正しく行われておらず，描画処理が正しく終了する前にタスク切り替えによって強制的に処理が終了されているのではないかと考えた．
---
</details>
<details>
<summary><big><b>14日目(2026-03-14), 第十四章</b></big></summary>

- **やったこと**
    - やるべきことのないタスクでも処理が追い付いていないタスクでも平等に時間が割り振られる設計になっていたので，タスクのスリープ状態を作成して，その間はキューに入らないようにして，必要なタスクにのみ時間を割り振るようにした．
    - 各タスクには本来優先度があり，今すぐにでもやらなければいけないタスクもあれば時間があるときにとりあえずやってくれればいいタスクもある．そのため，キューに優先度機能を追加し，すべてのタスクを平等に扱うラウンドロビン方式から優先順方式に切り替えた．
- **学んだこと**
    - 優先度付きキューによる現実に近いタスク管理の手法について学ぶことができた．
    - また，今回の場合はランニングキューが空になる，といった例外に対しても適切な処理を加えることで想定外の挙動を防ぐことが重要であるとわかった．例外のハンドルはシステムを設計・構築するときに必ず念頭に入れておかなければいけないことであり，これを疎かにするとのちに例外が積み重なっていき，手遅れな段階で重大な欠陥となって現れる可能性がある．
- **考察**
    - 今回やったことは，前回がマルチタスクの根幹の用意だとしたら，処理の中にある無駄の削減に終始しているといえる．やはり OS の役割は資源管理であり，このような作業を積み重ねて，適切な管理手法に改善することでパフォーマンスの向上が見込める．管理の考え方自体は正しくても，それに必要なコストも考えて本当に必要か考慮すべき．
    - 優先度付きキューは図にすると二次配列に近く，各行が優先度で，各列はその優先度のタスク一覧となっており，その優先度は，行が外側，列が内側の二重ループでの二次配列の各要素の探索順と同じであると理解した．ただし，優先度付きキューの考え方や実際の実装方法に関しては連結リストを用いるべきである．連結リストに比べ，二次配列による実装では各行(レベル)の要素数が同じである必要があることや，次の要素の参照ができないため，空の要素を飛ばして次の要素を探索するという処理が必要になってしまう．
    - 今回までに出てきた問題点は以下のとおりである．
        - タスクBをスリープした際に，マウスカーソルでタスクBの文字列をなぞると文字が消えてしまうことがる．
        - アイドリング状態やタスクBを稼働させた状態にすると，Hello Window の秒数カウントが50単位でしか動かなくなる．
        - 前回から継続してタスクBのドラッグでその残像が残る．
    
        一つ目の問題は，文字列情報がバックバッファとフレームバッファで異なり，カーソルでたどった部分からフレームバッファからバックバッファの文字列を再描画している状態に更新しているために起きていると考える．
        二つ目の問題は，マウスカーソルやテキストボックスに文字を打ち込んだりしてメインタスクにイベントを与えている間は正しく更新されていることから，イベントの割り込みによって解消できることが分かり，このことからメッセージ処理後にSleep状態になっていることが原因であると推測できる．また，50secごとに更新されるのはMessageのタイマが0.5秒単位でタイムアウトし，この時にMainWindowをWakeupしているためであると考えられる．
---
</details>
<details>
<summary><big><b>15日目(2026-03-15), 第十五章</b></big></summary>

- **やったこと**
    - ウィンドウの描画を，メッセージとしてキューに入れてメインスレッドがすべて担うように変更し，前回までの問題をすべて解決した．
    - 今の状態ではウィンドウの階層が固定されており，ウィンドウが重なった部分はドラッグしないと見えなかったが，アクティブウィンドウを採用し，アクティブなウィンドウをカーソルの一つ下のレイヤに移動させ，レイヤの階層移動を実現した．
    - キーボード入力がすべてテキストボックスに吸われていたが，アクティブウィンドウの実装によってアクティブなウィンドウへの命令としてキーボード入力を与えられるようにした．
    - ターミナルウィンドウを作成して，ターミナルっぽい黒いコンソール画面を作成した．
- **学んだこと**
    - 前回までの描画問題の解決方法は，排他制御を描画処理に挟む手法と，ウィンドウの描画を処理をする各タスクを一つのタスクにまとめる方法がある前者の手法は優先順位の逆転というOSのタスクスケジューリングの有名な問題の一つだと知った．
    - 子クラスで親クラスの関数をオーバーライドする際に，その処理としてまず親クラスの関数を呼び出しておくことで，今は親クラスの関数に何の役割もなくても，後でそれを改造して機能を追加した際に，その変更がオーバーライドした子クラスのメソッドにも反映され，柔軟な対応ができるようになるとわかった．
    - day15bの段階においてタスクBが起動時に一瞬描画され，そののちに透明化する不具合があった．だが，有志によってその原因と対策法が参考リポジトリの Issues に投稿されており，その通りに変更することで無事解決できた．
- **考察**
    - 今回は，ウィンドウの描画の役割をすべてメインスレッドに委譲したことで，タスクBのカウンタの進みが遅くなった．これによって描画のためにタスク切り替えの回数が向上し，オーバーヘッドが大きくなることが原因だと推定された．これまでマルチタスクの時間割り当ての効率化によって高速化を行ってきたが，不具合の修正のために速度を犠牲にする必要が生まれた．
    - ターミナルの追加によってその再描画に必要な処理が生まれ速度が落ちた．そのためターミナルでは再描画が必要な範囲をカーソルの位置のみに制限することで高速化を行った．このように新しい機能の追加によって遅延が生まれ，その遅延をできるだけ解消して高速化して次のステップに進む，の繰り返しでずっと進んでいる．
    - 今回行った画面の描画処理の一元管理や，アクティブタスクの実装によるキーボード入力対象の選定など，各タスクに対してその状況における明確な役割をあたえることで，それぞれのタスクが独立して動いている感覚が強くなってきた．
    - これまでにも s や w の入力によるタスクの Sleep・Wakeup を操作していたが，ターミナルウィンドウの実装によって本格的にキーボード操作による処理の実行機能が作成可能になってくると感じた．
---
</details>
<details>
<summary><big><b>16日目(2026-03-16), 第十六章</b></big></summary>

- **やったこと**
    - ターミナルに文字を打ち込めるようにした．
    - ターミナル上で文字を打ち込んで，コマンドを実行できるようにした．実装したコマンドは echo, clear, lspci, そして上下矢印の入力によるコマンド履歴の復元である．
    - 通常は1行で1コマンドであるものの，既存のターミナルに倣って複数コマンドを;でフラグメンテーションを行うことで，while 文を用いて記述されたコマンドを逐次実行するよう改変した．
- **学んだこと**
    - 今回やったことはほとんど過去にやったことの組み合わせで新しいことはあまりなかった．
    - 文字入力の実装はテキストボックスとコンソールの組み合わせで，echo, clear もただの画面描画．lspciも過去にやったpciデバイスの探索の再現だった．
    - 唯一コマンド履歴は，キューのpop_backとpush_frontを利用して過去に打ったコマンドを一定数記録しており，古いものを廃棄して新しいものを追加する形で実装しており，過去に用いた手法に近いものがあるものの目新しさはあった．
- **考察**
    - スタック機構は関数呼び出しなどにおけるOSの根本的なメモリ管理の面において裏側で多用していると思われるが，最近の作業ではキューを用いたタスク処理の効率化，過去の記憶などの利便性の向上に多用しており，現在はキューの重要性をより感じている．
    - 既存のターミナルにある | や >, < 等の特殊文字を用いた便利機能を追加していきたい．今はファイル機能がないが次章では実装するようなので，これらの機能があれば実際のOSに近いターミナルのさわり心地を再現できるように思う．
    - 今日の作業はOSの本懐である資源管理機能というよりもアプリケーション側に寄っているような気がしていて，見覚えのある手法や機能が多いため新鮮味が少なかった．
---
</details>
<details>
<summary><big><b>17日目(2026-03-17), 第十七章</b></big></summary>

- **やったこと**
    - バイト列に任意の名前を付けたものであるファイルとそれらを保持するディレクトリのうち，ボリュームイメージを，BPBの値からルートディレクトリの位置を取得した．それを UEFI BIOS の Block I/O Protocol によってブートローダでメモリ上に読みだし，それを OS に渡すようにした．
    - OS に渡されたボリュームイメージをルートディレクトリエントリと認識して，それに含まれるファイル名を全て表示する，ls コマンドを作成した．
- **学んだこと**
    - BPBやディレクトリエントリの構造とオフセットごとの役割を利用し，ボリュームイメージの構造を OS 側で表示する方法を理解した．
    - 現行のOSではそれぞれのブロックデバイスに対してドライバが必要となり，そのドライバを用いて自由に読み書きしていることを理解した．また，これらの開発は大変であるので，今回，OS は UEFI BIOS の機能でブートローダがメモリに書き出したデータを読んでいるだけにとどめていた．
    - ボリュームの読み込みを先頭 16 MiB に制限しているが，ボリュームのうち先頭には管理用データ構造があり，USB メモリの自体の容量によってある程度の大きさが決まる．そのため，16MiB 程度ではそのほとんどが管理用データ構造で埋め尽くされる恐れがあり，読み込むボリュームイメージ自体を大きくしたり，小型のメモリを採用して管理用データ構造を小さくすることがある．
- **考察**
    - ディレクトリエントリにはファイルの短命以外にも様々な情報が含まれるため，ls コマンドにオプションを作成してファイル属性なども提示できるようにすることなどが可能と考えられる．
    - あくまで自作 OS なので，ドライバの対応するブロックデバイスの種類は一つに絞っても問題ない気がする．そのため，経験としてドライバを一つ作成して，OS 側でそれを利用して自由に読み書きできるように改変できるようにしてもよい気がする．
    - デバイスの検知を考えるときに，今のところすべて UEFI BIOS の機能を用いて解決できている．そのため，今後自身で新しいデバイスを利用することを考えるときには，まずUEFI BIOSの仕様書を調べるようにしたい．
</details>
<details>
<summary><big><b>18日目(2026-03-18~19), 第十八章</b></big></summary>

- **やったこと**
    - ディレクトリエントリの情報から，引数として与えられたファイル名に一致するファイルを探索し，一致するファイルがあればその FAT を参照して中身を出力する cat コマンドを作成した．
    - アセンブリで作成したonlyhlt関数をボリュームイメージに格納し，ターミナル上でonlyhltと打つことで一致するファイルを探索してそれを実行する関数呼び出し(アプリケーション)機能を作った．
    - アセンブリでしかアプリケーションをかけないと不便なので，C++でもかけるようにして，引数を渡すことでそれを逆ポーランド記法として計算する rpn をC++で作成した．
- **学んだこと**
    - FATはクラスタチェーンの構造でできており，これによって，もし一つのオブジェクトが続く場合は次のクラスタ番号を指定し，オブジェクト末尾では0xfffffff8から0xffffffffを指定して終端を示す．
    - Cでの文字リテラルの記法として，"\x7f" "ELF"と書くと連結して末尾に NULL 文字を追加して認識されるうえ，"\x7fELF"と書くと\x7fEまでを16進数として認識することを防ぐことができるとわかった．
- **考察**
    - FAT のクラスタチェーン構造では次のクラスタを指名することでクラスタを形成している．この構造では，領域を確保する際に連続した空き領域を探索することなく，空いている領域を片っ端から埋めて各々に次のクラスタ番号を指定させることで効率的な管理方法となっているのではないかと考えた．
    - 現状の rpn ではスタックポインタの範囲確認を行っておらず，逆ポーランド記法の規約に従っていない式を書き込まれると不適切な値が返り値に設定されたりスタックアンダーフローを起こす．さらにその状態でpushを用いることで任意の数字列を書き込めてしまい，制御可能な状態でメモリを破壊できてしまう．
    - 現状の自作OSではブロックデバイスのデータを最初に一度だけOSにまとめて渡す形でボリュームイメージを読み込ませている．そのため，新しいアプリケーションを読み込ませるためには再起動が必要である．一方で，Ubuntu上でsudo apt installしてアプリケーションをインストールした際には再起動の必要なくそのまま実行できるようになる．これは先日述べたように現行のOSでは適切なタイミングで何度もドライバをもちいてブロックデバイスを参照していることが原因であり，これは現行のドライバでの読み込みの優位性が大きく感じられる部分である．
</details>
<details>
<summary><big><b>19日目(2026-03-19), 第十九章</b></big></summary>

- **やったこと**
    - ページング構造を作成した．
    - ページングを利用して，リンク時に決まる仮想アドレスにロードし，LOADセグメントを仮想アドレスに配置することで，前回最後に発生した標準ライブラリを利用したrpnコマンドを実行すると出力が狂う問題を修正した．
- **学んだこと**
    - これまで問題が発生していた原因は，ELF が想定する仮想アドレス配置と実行時の配置が一致していなかったためだと分かった．
    - ボリュームイメージに取り込んだ際にはアプリケーションファイルは空いている場所に適当に配置されため，正しくロードしないとコード内で指定している仮想アドレスとまったく別の場所に演算子が配置されてしまい，演算子比較の部分がでたらめな処理となる．
    - 過去のコードではうまくいった原因は，最適化の際に関数呼び出しが削除され， .rodata参照ではなく.text上で直接比較しているためにアドレス参照を行っておらず，ロード時のアドレスを考慮する必要がないためであった．
    - ページングは，実行するアプリケーションファイルのロードセグメントの配置に必要なページ数を計算し，その分のページを取得してpresentフラグを立てることで適宜有効化したのちLOADセグメントをコピーしていることが分かった．
    - C++では構造体制約を用いることで本文では個別の変数を[]を用いて構造体として認識させ，返り値を本文側の型にあった構造体とすることで疑似的に返り値を複数設定できるとわかった．
    - メモリ領域の無効化の際に，まず最初にその根であるpml4エントリと一度無効化してから再帰的に開放していくことで，無効化中にアプリが呼び出された際に破損したデータやエントリへの参照を予防している．
    - アプリケーションはあくまで仮想アドレスを指定すればよく，そこから先は OS が用意したページテーブルを MMU が参照することで自動変換している．
    - 自動変換してくれる理由は，過去にCR3をpml4テーブルの物理アドレスとしたことでCPU規約に則った形でページテーブルを実装しているためである．
    - 現在の実装では起動時は identity table を基盤として用い，アプリ実行時には4 KiB でのページングを追加して行っている．
    - Huge Page を利用する際はオフセットが21ビットに変更するなど，ページサイズに応じたオフセットとなることが分かった．
- **考察**
    - 現在のページングでは実行のために仮想アドレスにコピーしたLOADセグメントが実行中は一時的に2つ存在しメモリ使用量が増加するというコストが考えられる．
    - 短期間に複数回呼び出す場合はコピーと削除によるオーバーフローが懸念される．そこで，再利用の可能性を考慮して一時的にロードセグメントを保持して，再利用がなければそののちに無効化を行うことでオーバーヘッドを減らせるのではないかと考える．
    - また，現状はコンテキスト切り替え時にも CR3 に関してはコピーしており，単一アドレス空間で同一の利用仮想アドレスを前提としている．そのため，逐次実行の間は問題が表面化しにくいが，同時実行に拡張するとマッピング競合によってLOADセグメントが破損する可能性があると考える．
    - 現行 OS では，プロセスごとに異なるページテーブルを保有しており，コンテキストスイッチ時にページテーブルごと切り替えることで同一仮想アドレスでも異なる物理アドレスを指すようになり，アプリケーション間での競合が起きないような設計になっている．
    - 一方でCR3を切り替えるようにするということはそのままやるとカーネル領域まで切り替えてしまうことになる．そのため，共通領域の扱いや，ほかにもTLBによるアドレス変換キャッシュを随時クリアするなどの複雑な処理が必要になる．
    - 結果として速度の低下が考えられ，利便性と安全性を考慮した改変を行うためには，やはり速度が犠牲になることが考えられた．
    - 一般的にページングは今回の目的である適切な仮想空間の提供のほかに，効率的なメモリ利用，共有と再配置の用意化，複数プロセスの場合にはメモリ保護の目的がある．
    - 効率化はその原理からオーバヘッドとのトレードオフであり，不要なレベルの多段テーブル化等，やりすぎた効率化はかえって非効率化につながる．
    - アプリケーション開発者は仮想アドレスの競合を考えずに済んでいるのは OS 側での緻密なコンテキストスイッチのおかげであるといえる．
    - また，現段階ではとりあえず動く仕組みを考えるような資源管理から，壊れにくく，効率のいい資源管理を重視するようになっているように感じた．
</details>
<details>
<summary><big><b>20日目(2026-03-20), 第二十章</b></big></summary>

- **やったこと**
    - 実行したい関数が配置されている仮想アドレスを指定して関数を作成することで，アプリ側でカーネルで利用している関数を呼び出して画面描画を行った．
    - アプリ側がOS機能を呼び出せる状態は健全ではないので，GDTにユーザ用セグメントを追加し，アプリはそちらで実行することで，アプリ側からはOSの機能を使えないようにした．
    - 割り込みハンドラをasmで実装することで復帰位置を適切な位置に修正した．
    - ~~TSSとスタック領域が重複していてメモリ破壊が起きる可能性があるので，競合しないよう適切にTSSを初期化するようにした．~~TSS.RSP0を初期化しておらず0となっており，CPL3から割り込みでCPL0となる際に不正アクセスが発生する問題があった．そのため，カーネル起動時に適切にTSS.RSP0を初期化した．
    - 例外ハンドラを作成して，エラーの原因を追跡できるようにした．
    - システムコールを作成し，安全にOSの機能をアプリで使うための基盤を整えた．
- **学んだこと**
    - RSP0未初期化の問題で不正アクセスが起こる領域は，現在のアプリのコマンドライン引数が利用する領域として有効化（present=1）しているため，present=0による検出をすり抜けていたことが分かった．
    - 今まで気にしていなかったが，アプリ側でOSの機能が自由に使える状態はセキュリティ云々を語る土俵にすら立っていない状態だとわかった．
    - 確かにCでもOSの機能を使う際はシステムコールと呼ばれる用意された低水準関数を必ずどこかで用いており，アプリにはこれを利用させ，こちらが用意した処理のみを安全にアプリからカーネルへ委譲させることが，OSのセキュリティを考えると重要であるといえる．
    - 復帰先は利用するRIPによって決定し，C++などの高級言語ではコンパイルを行う都合上こちらで適切にRIPを選択することが難しく，asmを用いたほうが明示的に割り込みフレームのRIPを用いて適切に中断点に戻りやすくなる．
    - わたしは参考文献の著者が完成させたコードを写経して解読するだけであるが，現在のコードを完成させるまでには例外を潜り抜ける必要があることを再認識した．
- **考察**
    - 今回はasmで命令を記述することが多かったことから，コンテキスト管理等繊細な基盤部分の処理では，高級言語よりも低級言語でより粒度の細かい制御を行うことが大切だとわかった．
    - syscallを作成し，システムコールの土台となるファイルができたことで，これからはアプリからは呼ばれるべきではない機能とアプリでも必要な機能でパーティションを作成し，壊れにくい安定した挙動となることが望める．
    - デバッグ機能を追加して例外原因を追究できるようになったことで，やみくもな開発を減らし，問題部分の特定を効率的に行えるのだと感じた．
</details>
<details>
<summary><big><b>21日目(2026-03-21), 第二十一章</b></big></summary>

- **やったこと**
    - syscall実行中にタイマ割り込みを行うと，CPLが0のままのためにアプリスタック領域を使用してしまう状態だったため，TSSにIST用スタックを用意し，LAPICタイマ割り込みをIST1で受け取るようにして，メモリ破壊を防ぐようにした．
    - アプリ側から文字列をターミナルに表示させるためのシステムコールを作成した．
    - アプリ側が終了した際にOS側に戻る手段がなかったため，復帰のためのシステムコールを作成した．
    - ウィンドウを生成するシステムコールと，生成したウィンドウに文字列を書き込むシステムコールを作成した．
- **学んだこと**
    - 今回のように，アプリ内でもシステムコール中は一時的にCPL=0となっており，このとき割り込みが起きるとCPL遷移は発生せずRSPは自動で切り替えられず，場合によってはアプリスタックをそのまま利用してスタック構造が破壊されてしまう可能性があるとわかった．
    - ISTを用いることで，CPL遷移の有無に依存せず割り込み条件を指定することで強制的にその割り込み時に別スタックに切り替えられることが分かった．
    - 今回アプリ側ではprintfを利用しているが，これはシステムコールであるwriteのラッパーを定義し，OS側に描画処理を依頼できるようになったためであり，関数中の必要なシステムコールを伴う関数を何らかの形で定義しておけばそれを利用可能になるとわかった．
    - 現状のアプリ終了処理はアプリ側で呼び出す必要があり，終了システムコールを呼んでいない関数は残り続けるため，CPU例外を検知して，そのアプリのみを終了させる機能も必要であるとわかった．
- **考察**
    - 前回のTSS.RSP0や今回のISTの実装など，場合によっては現れていないところで想定していない挙動をしている場合があり，その制御のためにも緻密に状態遷移を計算し，各フラグ，アドレスの値が正しく設定されているのかを確認することが重要であると考えた．
    - これまで作成したシステムコールは既存のCに存在するexitやwriteに近く，現行のOSに近い，アプリにOSの機能へのIFを提供している状態が出来上がってきていると考える．
    - 現状のアプリではターミナルの入力を絶対的に信頼しているような処理体系がみられるため，引数の数の不一致などによってターミナルが停止してしまうことがあった．試しにlargeアプリで引数検証処理を追加をすると構文エラーでも停止を回避して終了できたためcd，このような処理を適宜追加していきたい．
</details>
<details>
<summary><big><b>22日目(2026-03-22), 第二十二章</b></big></summary>

- **やったこと**
    - アプリ側のsyscallIFをC/C++の双方に対応するように改造し，_exitによって既存のコンパイラのexitシステムコールを呼び出すことで同様の挙動を得られるようにした．
    - 四角形描画のsyscallを追加し，図形描画を実現した．
    - tick取得のsyscallを追加し，アプリで時間計測をできるようにした．
    - レイヤ描画の更新タイミングを設定するフラグを追加し，描画処理の最適化を行った．
    - 線分描画syscallを追加した．
    - ウィンドウ終了syscallと，イベント読み取りsyscallを追加し，Ctrl+Qが入力されたら起動しているwinhelloアプリを終了するようにした．
- **学んだこと**
    - 同じような図形・文字列描画syscallに対して，共通処理関数DoWinFuncを定義しておき，各syscall内で特徴的な処理部分をラムダ式で書いてから関数型の引数として渡すことで，スマートに処理をかけることが分かった．
    - 長方形描画syscallだけで全図形の描画が可能となるものの，それだけで実現するのは面倒なために，線分描画関数も実装して各描画関数で役割分担をすることが必要である．
    - やはり描画処理は比較的単体として重い処理なので，描画を内部でまとめて行ってから一気に画面に反映することで効率化につなげることができる．
- **考察**
    - 再描画を高頻度で行う場面では，syscallの呼び出し回数も増えやすく，単純な実装ではコンテキスト切り替えのオーバーヘッドが大きくなると考えられる．そのため，バッファを用いたバッチ処理で描画命令をまとめ，必要なタイミングでのみ再描画を行う設計が重要である．
    - 今回のウィンドウ終了syscallの実装を通じて，GUI上の表示状態と内部のメモリ状態を常に一致させることの重要性を再認識した．一致していない場合，画面上に不要な描画が残ったり，終了済みの処理に関連する情報が取り残されたりして，不具合の温床になると考えられる．
</details>
<details>
<summary><big><b>23日目(2026-03-23), 第二十三章</b></big></summary>

- **やったこと**
    - マウスの移動イベントをアプリ側で取得できるようにして，マウス位置を追跡するeyeアプリを作った．
    - マウスのボタンイベントをアプリに渡せるようにして，左クリック中に線をかけるpaintアプリを作成した．
    - CreateTimer syscallを追加することでタイマタイムアウトをアプリで使えるようにして，指定時間待機するtimerアプリを作成した．
    - キーボード入力で押下に加えて，離上も判別できるようにして，ReadEventでキーの入力イベントをより詳細に扱えるようにした．これに伴って，ブロック崩しアプリblocksを作成した．
- **学んだこと**
    - 今回はOS機能というよりもこれまでに作ったものを利用したアプリの作成によっていたので特になし．
- **考察**
    - 今回はアプリ作成の比重が大きい一方で，OS側では「入力イベントをどの粒度でアプリに渡すか」というIF設計の重要性を強く感じた．マウス移動，マウスボタン，タイマ，キー押下／離上をイベントとして統一的に扱えるようにしたことで，アプリ側は描画ロジックに集中できる構造になってきた．
    - キー入力の押下と離上を区別して扱えるようになったことで，詳細な入力管理が実現でき，誤入力や多重処理を防ぐことや入力処理の安定性を上げる基盤になると考える．
    - CreateTimer syscallの追加によって，アプリ側が時間を自律的に扱えるようになった．これによってアプリ側でも時間を用いた処理フローの管理を行えるようになり，イベント駆動での負荷軽減を県道できるようになった．
    - 現在行っている作業は，OSの役割の一つである，OSが用いる機能のうち，アプリ側で使っていいものを選別して，その機能をアプリ側で安全に扱えるようなIF基盤を作成することである．
</details>
<details>
<summary><big><b>23日目(2026-03-23), 第二十三章</b></big></summary>

- **やったこと**
    - ターミナルを複数同時起動できるようにした，
    - ターミナルを同時に起動してアプリを複数同時に立ち上げた際に，仮想アドレスが競合してしまいクラッシュする．そのため，各ターミナルごとにPML4テーブルを作成して個別の階層ページング構造を実装して競合を防いだ．
    - ウィンドウの作成時に，最前面に描画されるように修正した．
    - 画面表示を行わない別ターミナル上でアプリを起動させる，&コマンドに似た効果を持つnotermコマンドを作成した．
    - アプリケーション(CPL=3)でCPU例外が発生したときに，OS全体がフリーズしないようにアプリを強制終了させる機能を作成．
- **学んだこと**
    - これまでは単一ターミナルを想定していたため，メインタスクでタイマの管理を行っていたが，複数のターミナルを用いる際にはそれぞれタイマの管理を行うようにした．機能的な部分を考えて各々の役割を決めることが重要である．
    - 複数のターミナルの管理を行う際には各々で独立したメモリ管理体系を持つことが大切である．今回はこれまでにタスクごとのコンテキスト切り替えと，階層ページング構造の実装，カーネル領域の独立を行っていたため，コンテキスト切り替え時にルートページテーブルを切り替えて別の階層ページング構造を利用するように変更することで各タスクのメモリ領域の独立を実現した．
    - アプリで例外を発生させるとそのままOS全体が停止してしまう構造はよろしくないので，例外を検知してその発生源(アプリ)を終了させて切り捨てる実装が必要である．
- **考察**
    - 一つのアプリを起動させられる状態と，複数のアプリを同時に起動，操作できる状態には大きな差があると感じた．複数を管理する場合は，各プロセスを安全に切り離して独立させる必要がある．これを怠ると一つのアプリの障害が他すべてに波及したリ，他プロセスとのメモリ領域の競合でクラッシュを起こす可能性がある．
    - 今回も，新しい機能の追加とそれに伴う不具合の修正が主な作業であり，特に例外にロバストな構造とすることを重点に置いた回であったと感じた．
    - notermは見た目上は&と挙動が似ている一方で，&は同一ターミナル内のジョブ管理（待機方式の変更）であるのに対し，notermは実行文脈そのものを別ターミナルとして分離する点が本質的に異なる．この違いにより，UIの有無と実行管理を切り離して設計でき，障害の局所化や今後のジョブ制御拡張（jobs/fg/bg相当）につながる基盤になると考える．
</details>

## Credits
This project is based on the book:
- **"ゼロからのOS自作入門" (MikanOS)** by [Kouta Uchida (uchan-nos)](https://github.com/uchan-nos/mikanos)
- Some build tools and scripts are derived from [osbook/devenv](https://github.com/uchan-nos/mikanos-build).
